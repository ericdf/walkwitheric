{
  "name": "walkwitheric",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy",
    "clean": "rm -rf _site",
    "crop": "python3 scripts/process_images.py",
    "schedule": "node --env-file=.env scripts/fetch-schedule.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@tailwindcss/postcss": "^4.2.4",
    "postcss": "^8.5.13",
    "tailwindcss": "^4.2.4"
  }
}
